<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Full-screen live preview WebView (background) -->
    <WebView
        android:id="@+id/preview_webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Semi-transparent vertical control panel on the right -->
    <LinearLayout
        android:id="@+id/control_panel"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:orientation="vertical"
        android:background="#CC1a1a2e"
        android:padding="12dp">

        <!-- Header: title + close/done buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/layout_editor_title"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btn_reset"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="@string/layout_editor_reset"
                android:textColor="#FFFFFF"
                android:textSize="11sp"
                android:backgroundTint="#88c0392b"
                android:minWidth="0dp"
                android:paddingHorizontal="12dp"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:nextFocusRight="@id/btn_done"
                style="?android:attr/buttonBarButtonStyle" />

            <Button
                android:id="@+id/btn_done"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="@string/layout_editor_done"
                android:textColor="#FFFFFF"
                android:textSize="11sp"
                android:backgroundTint="#8827ae60"
                android:minWidth="0dp"
                android:paddingHorizontal="12dp"
                android:layout_marginStart="6dp"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:nextFocusLeft="@id/btn_reset"
                style="?android:attr/buttonBarButtonStyle" />
        </LinearLayout>

        <!-- Element selector tabs (horizontal scroll) -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            android:layout_marginBottom="8dp">

            <LinearLayout
                android:id="@+id/element_tabs"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal" />
        </HorizontalScrollView>

        <!-- Slider controls for selected element -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:fadeScrollbars="true">

            <LinearLayout
                android:id="@+id/slider_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </ScrollView>

    </LinearLayout>

</FrameLayout>
